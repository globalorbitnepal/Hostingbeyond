export const MIGRATION_FRAME_SPECS = {
  heroComposite:
    "1280 × 720 px (16:9) — full right-column Hostinger-style collage",
  heroPortrait:
    "800 × 1000 px (4:5) — portrait-only fallback if not using composite",
  stepsImage:
    "960 × 640 px (3:2) — optional image beside “how it works” on large screens",
} as const;
