export const MIGRATION_FRAME_SPECS = {
  heroPortrait:
    "800 × 1000 px (4:5) — center portrait in hero collage; waist-up, neutral background",
  heroVisual: "1200 × 720 px (5:3) — optional full hero replacement (legacy)",
  stepsImage:
    "960 × 640 px (3:2) — optional image beside “how it works” on large screens",
} as const;
