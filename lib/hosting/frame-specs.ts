/** Image upload hints for Orbit → Web Hosting (minimal imagery on this page). */
export const HOSTING_FRAME_SPECS = {
  heroOptional:
    "1600 × 900 px (16:9) — optional hero visual (leave empty for text-only hero)",
  wordpressBand: "1200 × 640 px (≈16:9) — optional WordPress band background",
} as const;
